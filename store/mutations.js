export function setSearchMeals(state, meals) {
  state.searchMeals = meals;
}

export function setMealByLetters(state, meals) {
  state.mealsByLetters = meals;
}

export function setMealByIngredients(state, meals) {
  state.mealsByIngredients = meals;
}
